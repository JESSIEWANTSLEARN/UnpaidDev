<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed development data here when a dedicated WalangBrownout seeder is added.
     * Production data must never be embedded in source control.
     */
    public function run(): void
    {
        // Intentionally empty.
    }
}
